// Import a library to help create a Component
import { AppRegistry } from 'react-native';
import App from './src/app';

// Create a Component

// Render in to the device
AppRegistry.registerComponent ('auth', () => App);
